# bootstrap/argo-cd-install.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: argocd
---
# Install ArgoCD core components
# (official upstream manifest - lightweight)
# You can replace with full if you want UI, RBAC, etc.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
