apiVersion: v1
kind: Service
metadata:
  name: ledger-backend-service
spec:
  selector:
    app: ledger-backend   # matches backend deployment labels
  ports:
    - port: 80            # service port (what other pods will use)
      targetPort: 2231    # actual backend container port
  type: ClusterIP         # internal-only access (best practice)